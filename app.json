{
  "name": "OpenTokRTCV2",
  "description": "OpenTok RTC sample application to show off the OpenTok API and platform capabilities ",
  "keywords": [
    "WebRTC",
    "HTML5",
    "productivity"
  ],
  "website": "https://tokbox.com/",
  "repository": "https://github.com/opentok/OpentokRTC-V2.git",
  "logo": "https://tokbox.com/developer/img/OT_logo.png",
  "success_url": "/",
  "scripts": {
    "postdeploy": "bower install && grunt clientBuild"
  },
  "env": {
    "TB_API_KEY": {
      "description": "OpenTok API key: Login to the Tokbox Dashboard (https://dashboard.tokbox.com/keys) to get this value.",
      "required": true
    },
    "TB_API_SECRET": {
      "description": "OpenTok API secret: Login to the Tokbox Dashboard (https://dashboard.tokbox.com/keys) to get this value.",
      "required": true
    },
    "ENABLE_ARCHIVING": {
      "description": "Set to 'false' if you want to disable archiving.",
      "value": "true",
      "required": false
    },
    "ENABLE_ARCHIVE_MANAGER": {
      "description": "Set to 'true' if you want to enable archiving manager, used to store archiving information. If set to 'true', need Firebase access credentials to be put in following fields.",
      "value": "false",
      "required": false
    },
    "FB_API_KEY": {
      "description": "Firebase API key",
      "required": false
    },
    "FB_DATA_URL": {
      "description": "URL of the Firebase app's database used to store the archive information. Go to https://firebase.com to get a valid URL.",
      "required": false
    },
    "FB_CREDENTIAL": {
      "description": "Content of private key for your Firebase application's service account",
      "required": false
    },
    "FB_IOS_APP_ID": {
      "description": "Firebase iOS app ID. This is the value of 'GOOGLE_APP_ID' key in Firebase's iOS configuration (.plist) file.",
      "required": false
    },
    "FB_IOS_SENDER_ID": {
      "description": "Firebase iOS sender ID. This is the value of 'GCM_SENDER_ID' key in Firebase's iOS configuration (.plist) file.",
      "required": false
    }
  },
  "addons": [
    "heroku-redis"
  ]
}
